### This is Graphin 
service for graph visualization of user funnel steps


1. Install requirements (needs pip)
```shell
pip install -r requirements.txt
```

2. Run project 
```shell
 streamlit run app.py
```

You can also make sample data
```shell
python3 generate_test_data.py
```